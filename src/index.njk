---
layout: base.njk
title: Home
permalink: /
---

{% block content %}
<section class="text-center py-12 bg-white rounded-lg mb-12">
  <h2 class="text-4xl sm:text-5xl font-bold mb-4 text-gray-900">Welcome to The Names Website</h2>
  <p class="text-xl text-gray-600 max-w-2xl mx-auto">Explore the origins, meanings, and stories behind names from around the world.</p>
</section>

<section class="mb-12">
  <h3 class="text-3xl font-bold mb-6 text-gray-900">Featured Names</h3>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
    {% for nameData in names | slice(0, 6) %}
    <div class="name-card bg-white">
      <h4 class="text-xl font-semibold mb-2">
        <a href="/names/{{ nameData.name | lower | replace(' ', '-') }}/" class="text-primary hover:text-blue-700">{{ nameData.name }}</a>
      </h4>
      <p class="text-sm text-gray-600 italic mb-2">{{ nameData.origin }}</p>
      <p class="text-gray-700">{{ nameData.meaning }}</p>
    </div>
    {% endfor %}
  </div>
  <p class="text-center">
    <a href="/names/" class="text-lg text-primary hover:text-blue-700 font-medium">View all {{ names | length }} names &rarr;</a>
  </p>
</section>

<section class="bg-white rounded-lg p-8">
  <h3 class="text-2xl font-bold mb-4 text-gray-900">About</h3>
  <p class="text-gray-700 leading-relaxed">This website is dedicated to exploring the fascinating world of names. Each name has a unique history, cultural significance, and meaning worth discovering.</p>
</section>
{% endblock %}
