---
permalink: /tags/
---

{% extends "base.njk" %}

{% block title %}Name Tags - {{ siteConfig.title }}{% endblock %}
{% block description %}Explore names by tags based on their historical popularity patterns across different eras{% endblock %}

{% block content %}
<article class="bg-white rounded-lg p-8 shadow-sm">
  <h1 class="text-4xl font-bold mb-4 text-gray-900">Name Tags</h1>
  <p class="text-lg text-gray-600 mb-8">
    Names are tagged based on their popularity patterns across different historical eras. These tags help identify names that were exclusively popular in specific time periods or have experienced revivals.
  </p>

  <div class="grid md:grid-cols-1 lg:grid-cols-2 gap-6">
    {% for tag in tags %}
    <a href="/tags/{{ tag.slug }}/"
       class="block p-6 bg-gradient-to-br from-purple-50 to-indigo-50 hover:from-purple-100 hover:to-indigo-100 rounded-lg border-l-4 border-purple-500 shadow-md hover:shadow-lg transition-all duration-200 transform hover:scale-102">
      <h2 class="font-bold text-2xl text-purple-900 mb-3">{{ tag.name }}</h2>
      <p class="text-sm text-gray-700 mb-3 leading-relaxed">{{ tag.description }}</p>
      <div class="flex justify-between items-center">
        <p class="text-sm font-semibold text-purple-600">{{ tag.count }} names</p>
        <span class="text-purple-400 text-xl">&rarr;</span>
      </div>
    </a>
    {% endfor %}
  </div>

  <nav class="pt-8 mt-8 border-t border-gray-200">
    <a href="/" class="text-blue-600 hover:text-blue-700 font-medium">&larr; Back to home</a>
  </nav>
</article>
{% endblock %}
